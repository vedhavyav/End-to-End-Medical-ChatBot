from setuptools import find_packages, setup

setup(
    name = "GenAI Project", 
    version = '0.0.0',
    author="###", 
    author_email='ifeubc@', 
    packages= find_packages(), 
    install_requires = []
)